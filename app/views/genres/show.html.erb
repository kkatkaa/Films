<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @genre.name %>
</p>

<p>
  <strong>Movies</strong>
    <% @genre.movies.each do |movie| %>
      <%= link_to movie.title, movie_path(movie),  class: "btn btn-warning" %>
    <% end %>
</p>

<%= link_to 'Edit', edit_genre_path(@genre) %> |
<%= link_to 'Back', genres_path %>
